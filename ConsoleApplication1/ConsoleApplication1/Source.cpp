#include <iostream>
#include <math.h>

int main(int argc, char **argv)
{
	int c, b, a = c = b = 3;
	cin >> a >> b >> c;
	int delta = b*b - 4 * a*c;
	if (delta < 0)
		cout << "no results !\n";
	else {
		cout <<"0="
	}

	std::cout << "a+b";
	return 0;
}